import React from 'react';
interface FormTextareaProps {
    name: string;
    label: string;
    maxLength: number;
}
export declare const FormTextarea: ({ name, label, maxLength }: FormTextareaProps) => React.JSX.Element;
export {};
